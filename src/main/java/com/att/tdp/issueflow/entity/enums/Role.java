package com.att.tdp.issueflow.entity.enums;

/**
 * Role: Enum representing the security roles available for users in the system.
 * It distinguishes between ADMINs with full privileges and standard DEVELOPERs.
 */
public enum Role {
    ADMIN,
    DEVELOPER
}
